{
  "name": "velnar",
  "version": "2.3.1",
  "private": true,
  "description": "VELNAR — Make GitHub yours.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "icons": "node scripts/generate_icons.cjs",
    "build": "python scripts/build_zip.py"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": ">=20"
  }
}
